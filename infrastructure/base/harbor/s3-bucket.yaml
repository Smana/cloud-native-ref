apiVersion: s3.aws.upbound.io/v1beta1
kind: Bucket
metadata:
  name: harbor
  namespace: harbor
  annotations:
    crossplane.io/external-name: ${region}-ogenki-harbor
spec:
  forProvider:
    region: ${region}

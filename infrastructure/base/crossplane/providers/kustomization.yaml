apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - activation-policy.yaml
  # The cloud-neutral aggregate ClusterRoles, shared with providers-gcp.
  - ../rbac
  # AWS-only: the provider-aws ClusterRole and its binding.
  - additional-rbac.yaml
  - deploymentruntimeconfig-aws.yaml
  - provider-ec2.yaml
  - provider-eks.yaml
  - provider-iam.yaml
  - provider-kms.yaml
  - provider-s3.yaml

kcl_options:
  - key: params
    value:
      oxr:
        metadata:
          name: "xplane-mycache"
          namespace: "apps"
        spec:
          size: "small"
          replicas: 2
          auth:
            existingSecret: "mycache-valkey-password"
            passwordKey: "password"
          persistence:
            size: "4Gi"
          metrics: true
      ocds: {}

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: karpenter

# GPU NodePool gated by the LLM umbrella Flux Kustomization
# (see clusters/mycluster-0/llm-platform.yaml). Default: not applied
# unless the umbrella is resumed with `flux resume kustomization llm-platform`.
resources:
  - gpu-l4-nodepool.yaml
  - gpu-l4-ec2nc.yaml

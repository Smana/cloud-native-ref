apiVersion: s3.aws.m.upbound.io/v1beta1
kind: Bucket
metadata:
  name: cnpg-backups
  annotations:
    crossplane.io/external-name: ${region}-ogenki-cnpg-backups
spec:
  forProvider:
    region: ${region}

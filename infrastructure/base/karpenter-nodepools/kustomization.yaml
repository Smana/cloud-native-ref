apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: karpenter

resources:
  - default-nodepool.yaml
  - default-ec2nc.yaml
  - io-nodepool.yaml
  - io-ec2nc.yaml
  # GPU NodePool (gpu-l4-*.yaml) lives in karpenter-nodepools-gpu/ and is
  # gated by the LLM umbrella Kustomization (see clusters/mycluster-0/llm-platform.yaml).

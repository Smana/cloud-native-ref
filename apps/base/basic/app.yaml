apiVersion: cloud.ogenki.io/v1alpha1
kind: App
metadata:
  name: xplane-podinfo
  namespace: apps
spec:
  # Minimum required: container image
  image:
    repository: stefanprodan/podinfo
    tag: "6.15.0"

  # Service configuration
  service:
    port: 9898

  # HTTP routing (enable external access)
  # Default: private gateway (podinfo on the cluster private domain)
  route:
    enabled: true
    hostname: "podinfo"

apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: victoria-logs
  namespace: observability
spec:
  releaseName: victoria-logs
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: victoria-logs-cluster
      sourceRef:
        kind: HelmRepository
        name: victoria-metrics
        namespace: observability
      version: "0.2.8"
  interval: 2m0s
  timeout: 10m
  install:
    remediation:
      retries: 3
  valuesFrom:
    - kind: ConfigMap
      name: vl-common-helm-values
      valuesKey: values.yaml
  values:
    printNotes: false

    vlselect:
      horizontalPodAutoscaler:
        enabled: true
        maxReplicas: 10
        minReplicas: 2
        metrics:
          - type: Resource
            resource:
              name: cpu
              target:
                type: Utilization
                averageUtilization: 70

      podDisruptionBudget:
        enabled: true
        minAvailable: 1

      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: "app"
                    operator: In
                    values:
                      - "vlselect"
              topologyKey: "kubernetes.io/hostname"
      topologySpreadConstraints:
        - labelSelector:
            matchLabels:
              app: vlselect
          maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway

      resources:
        limits:
          cpu: 100m
          memory: 200Mi
        requests:
          cpu: 100m
          memory: 200Mi

      # Container-level securityContext. In the VictoriaMetrics charts the
      # `securityContext` key is the CONTAINER context (`podSecurityContext` is
      # the pod one); the chart default only sets runAsNonRoot/runAsUser/
      # runAsGroup and omits allowPrivilegeEscalation, which the constitution
      # mandates. Merged with, not replacing, those defaults.
      securityContext:
        allowPrivilegeEscalation: false

      vmServiceScrape:
        enabled: true

    vlinsert:
      horizontalPodAutoscaler:
        enabled: true
        maxReplicas: 10
        minReplicas: 2
        metrics:
          - type: Resource
            resource:
              name: cpu
              target:
                type: Utilization
                averageUtilization: 70

      podDisruptionBudget:
        enabled: true
        minAvailable: 1

      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: "app"
                    operator: In
                    values:
                      - "vlinsert"
              topologyKey: "kubernetes.io/hostname"
      topologySpreadConstraints:
        - labelSelector:
            matchLabels:
              app: vlinsert
          maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway

      resources:
        limits:
          cpu: 100m
          memory: 200Mi
        requests:
          cpu: 100m
          memory: 200Mi

      # Container-level securityContext — see the vlselect block above.
      securityContext:
        allowPrivilegeEscalation: false

      vmServiceScrape:
        enabled: true

    vlstorage:
      # -- Enable deployment of vlstorage component. StatefulSet is used
      enabled: true
      retentionPeriod: 7d
      retentionDiskSpaceUsage: "9GiB"
      replicaCount: 3

      podDisruptionBudget:
        enabled: true
        minAvailable: 1

      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: "app"
                    operator: In
                    values:
                      - "vlstorage"
              topologyKey: "kubernetes.io/hostname"
      topologySpreadConstraints:
        - labelSelector:
            matchLabels:
              app: vlstorage
          maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway

      persistentVolume:
        enabled: true
        size: 10Gi

      resources:
        limits:
          cpu: 500m
          memory: 512Mi
        requests:
          cpu: 500m
          memory: 512Mi

      # Container-level securityContext — see the vlselect block above.
      securityContext:
        allowPrivilegeEscalation: false

      vmServiceScrape:
        enabled: true

    vector:
      enabled: true

      # Vector subchart: `securityContext` is the CONTAINER context
      # ("Specify securityContext on Vector containers" — vector values.yaml).
      # Vector tails host log files as root; it never needs to escalate.
      securityContext:
        allowPrivilegeEscalation: false

      # The shared pipeline (vl-common-helm-values) defaults its two PG sinks
      # to the vlsingle Service; in cluster mode ingestion goes through
      # vlinsert, so both URIs are overridden here. General (non-PG) logs need
      # no override: the cluster chart generates its own `vlogs` catch-all sink
      # already pointed at vlinsert.
      customConfig:
        sinks:
          victorialogs_pg_plans:
            uri: http://victoria-logs-victoria-logs-cluster-vlinsert.observability.svc.cluster.local:9481/insert/jsonline
          victorialogs_pg_parse_failures:
            uri: http://victoria-logs-victoria-logs-cluster-vlinsert.observability.svc.cluster.local:9481/insert/jsonline

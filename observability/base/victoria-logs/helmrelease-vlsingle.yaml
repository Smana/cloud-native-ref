apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: victoria-logs
  namespace: observability
spec:
  releaseName: victoria-logs
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: victoria-logs-single
      sourceRef:
        kind: HelmRepository
        name: victoria-metrics
        namespace: observability
      version: "0.13.9"
  interval: 4m0s
  timeout: 30m
  install:
    remediation:
      retries: 3
  valuesFrom:
    - kind: ConfigMap
      name: vl-common-helm-values
      valuesKey: values.yaml
  values:
    printNotes: false

    server:
      resources:
        limits:
          cpu: 500m
          memory: 512Mi
        requests:
          cpu: 500m
          memory: 512Mi

      # Deliberate values (2026-08-30). Retention was previously unset — whatever
      # the app defaulted to. NOTE the units trap: a unit-less retentionPeriod is
      # MONTHS in VictoriaMetrics charts; always write the suffix.
      retentionPeriod: 7d
      retentionDiskSpaceUsage: 8GiB
      persistentVolume:
        enabled: true
        size: 10Gi
        storageClassName: ${storage_class}

      vmServiceScrape:
        enabled: true

    vector:
      enabled: true

      # Vector subchart: `securityContext` is the CONTAINER context
      # ("Specify securityContext on Vector containers" — vector values.yaml);
      # `podSecurityContext` is the pod one. Vector tails host log files as root
      # but never needs to escalate privileges.
      # (The `server` component already ships allowPrivilegeEscalation: false as
      # a chart default, so it needs nothing here.)
      securityContext:
        allowPrivilegeEscalation: false

      # The Vector pipeline (customConfig) is shared with the vlcluster variant
      # via the vl-common-helm-values ConfigMap (valuesFrom above).

    dashboards:
      enabled: true
      grafanaOperator:
        enabled: true
        spec:
          allowCrossNamespaceImport: true
          folderRef: "logs"

apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: crds-gitlab-operator
spec:
  interval: 10m
  targetNamespace: kube-system
  sourceRef:
    kind: GitRepository
    name: gateway-api
  path: "./deploy/chart/crds"
  prune: true
  timeout: 1m

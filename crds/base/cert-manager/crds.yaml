apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: crds-cert-manager
spec:
  interval: 2m
  targetNamespace: security
  sourceRef:
    kind: GitRepository
    name: cert-manager
  path: "./deploy/crds"
  prune: true
  timeout: 1m

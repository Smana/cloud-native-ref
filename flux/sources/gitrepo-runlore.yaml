apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: runlore
  namespace: runlore
spec:
  interval: 1h0m0s
  url: https://github.com/Smana/runlore.git
  # Pinned to the v0.9.0 release tag — reproducible: chart 0.9.0 (includes #307 the
  # HelmRelease watcher + #308 the VMServiceScrape dedup) paired with the released
  # multi-arch image ghcr.io/smana/runlore:v0.9.0 (helmrelease.yaml image.tag).
  # Switched off rolling main/:main now that release-please cut v0.9.0.
  ref:
    tag: v0.9.0

apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: runlore
  namespace: runlore
spec:
  interval: 1h0m0s
  url: https://github.com/Smana/runlore.git
  # VALIDATION DEPLOY — pinned to main HEAD (commit 010d33d, "feat: source_diff … #359")
  # to also validate M4 source_diff (source-repo whitelist → diff an app's source between
  # the two versions an image bump named). Previously validated at 26723739: D2 sweeps
  # (#370), M1 LogsProvider (#372), M3 Argo (#371), D1 kb import (#369), F1/F2.
  #
  # The chart (deploy/helm/runlore) is checked out from THIS commit; the image tag in
  # observability/base/runlore/helmrelease.yaml is ":main" (rolling amd64 build-image.yml
  # build for this commit). NOT a release: no v0.11.0 tag yet. `branch: main` lets
  # source-controller shallow-clone efficiently; `commit` takes precedence.
  ref:
    branch: main
    commit: 010d33d0230fa2a780daeaac767436b9db9f6023  # pragma: allowlist secret (git SHA, not a secret)

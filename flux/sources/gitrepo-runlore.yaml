apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: runlore
  namespace: runlore
spec:
  interval: 1h0m0s
  url: https://github.com/Smana/runlore.git
  # TEMPORARY: pinned to the runlore#308 fix branch (main 00e9b37 + one chart
  # commit) to pick up fix(chart): scope VMServiceScrape to the metrics Service —
  # the StatefulSet headless Service was being scraped too, double-counting every
  # counter metric. The rolling image ghcr.io/smana/runlore:main is unchanged
  # (chart-only change). RE-PIN to main HEAD once #308 merges.
  ref:
    branch: fix/vmservicescrape-headless-dedup
    commit: cdf122cfa5b9ad084667e3eae158e92ef7223475  # pragma: allowlist secret

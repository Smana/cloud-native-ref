apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: runlore
  namespace: runlore
spec:
  interval: 1h0m0s
  url: https://github.com/Smana/runlore.git
  # Pinned to main HEAD, which now includes runlore#263 (StatefulSet chart
  # support, squash-merged — feat/statefulset-ha branch is deleted). The rolling
  # image ghcr.io/smana/runlore:main still applies unchanged; only the chart at
  # deploy/helm/runlore differs from the previous pin.
  ref:
    branch: main
    commit: 9139d328809cc7280d1f022101d08801e2107b58  # pragma: allowlist secret

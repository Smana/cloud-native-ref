apiVersion: notification.toolkit.fluxcd.io/v1
kind: Alert
metadata:
  name: otel-traces
  namespace: flux-system
spec:
  providerRef:
    name: otel-traces
  eventSeverity: info
  eventSources:
    - kind: GitRepository
      name: "*"
    - kind: Kustomization
      name: "*"
    - kind: HelmRelease
      name: "*"

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: homepage
  namespace: tooling
spec:
  parentRefs:
    - name: platform-tailscale
      namespace: infrastructure
  hostnames:
    - "home.priv.${domain_name}"
  rules:
    - backendRefs:
        - name: homepage
          port: 3000

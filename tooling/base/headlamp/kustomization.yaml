apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tooling
resources:
  - flux-rbac.yaml
  - httproute.yaml
  - helmrelease.yaml
  - source.yaml

configMapGenerator:
  - name: deploy-plugins
    files:
      - ./deploy-plugins.sh

generatorOptions:
  disableNameSuffixHash: true

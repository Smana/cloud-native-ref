apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base/headlamp
  - ../base/harbor
  - ../base/homepage
  - ../base/pev2
  # Promptfoo nightly eval CronJob is gated behind the LLM platform
  # umbrella (its eval target is the Semantic Router, which only exists
  # when the LLM stack is enabled). Wired via
  # clusters/mycluster-0-llm-platform/tooling-promptfoo.yaml.
  # Enabling only when needed for cost reasons
  # - ../base/dagger-engine
  # - ../base/gha-runners

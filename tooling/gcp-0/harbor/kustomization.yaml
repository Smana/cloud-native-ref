apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tooling

resources:
  - ../../base/harbor
  - gcs-bucket.yaml
  - workloadidentity.yaml

patches:
  - path: storage-gcs.yaml
    target:
      kind: HelmRelease
      name: harbor

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: security

# The base is cloud-neutral: kms.yaml and s3-bucket.yaml -- AWS Crossplane
# resources (kms.aws.m.upbound.io, s3.aws.m.upbound.io) -- live here instead,
# alongside the shared CronJob/ServiceAccount/ExternalSecret/network policy.
resources:
  - ../../base/openbao-snapshot
  - kms.yaml
  - s3-bucket.yaml

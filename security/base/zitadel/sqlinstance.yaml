apiVersion: cloud.ogenki.io/v1alpha1
kind: SQLInstance
metadata:
  name: xplane-zitadel
spec:
  instances: 2
  size: "small"
  storageSize: "20Gi"
  postgresVersion: "18"
  createSuperuser: true
  # Recovery source for the next bootstrap (only consulted during
  # initial provisioning — `bootstrap` is immutable post-create).
  #
  # Frozen-dated-snapshot pattern (matches the prior zitadel-20241202
  # / zitadel-20251028 / zitadel-20260201 / zitadel-20260505 /
  # zitadel-20260719 history): `zitadel-20260829` is a copy of the live
  # prefix `xplane-zitadel-cnpg-cluster/` taken on 2026-08-29 after a
  # one-shot Backup (108 objects / 46 MiB, base backup + full WAL chain,
  # object count and byte size verified equal to the source).
  #
  # Promoted because the OIDC clients changed materially that day: the
  # `platform` project and its five clients (grafana, headlamp, flux-ui,
  # headlamp-proxy, harbor) were created, and the `admin` role granted.
  # The previous pin, zitadel-20260719, predates all of it -- restoring
  # from it would have silently reinstated the pre-refactor clients whose
  # redirect URIs still name priv.cloud.ogenki.io and mycluster-0, which
  # is exactly the breakage this rotation exists to prevent.
  #
  # NOTE: this was a COPY, so the live prefix is NOT empty. A restore
  # therefore still needs scripts/cnpg-prepare-restore.sh first (see the
  # expected-empty-archive note below). Earlier rotations MOVED the
  # prefix and got that clearing for free.
  #
  # The live prefix `xplane-zitadel-cnpg-cluster/` accrues daily
  # ScheduledBackups but is NOT used for recovery — that decoupling
  # means a bad day on the live cluster (corruption, accidental DROP)
  # doesn't cascade into recovery from a poisoned state.
  #
  # Promotion procedure (rotate the frozen snapshot when zitadel
  # changes meaningfully — new OAuth apps, schema migration,
  # significant user growth):
  #   1. `kubectl create -f one-shot-backup.yaml -n security`
  #      (Backup CR pointing at xplane-zitadel-cnpg-cluster).
  #   2. Wait for phase=completed.
  #   3. `aws s3 cp --recursive
  #      s3://eu-west-3-ogenki-cnpg-backups/xplane-zitadel-cnpg-cluster/
  #      s3://eu-west-3-ogenki-cnpg-backups/zitadel-$(date +%Y%m%d)/`
  #   4. Update this `path:` to the new dated prefix, commit, push.
  #
  # Follow-up: the original 2026-05-04 manual backup didn't write to
  # S3 (root cause unknown — suspect cluster destroy raced backup
  # finalize). Add a Flux pre-flight check that fails closed if the
  # configured recovery prefix is empty.
  # RESTORING REQUIRES CLEARING THE LIVE ARCHIVE FIRST, and on aws-0 that step is
  # already part of how rebuilds are done by hand -- the `…-cnpg-cluster/` prefix
  # is cleaned before the bootstrap, which is why it has always worked here.
  #
  # It is written down now because it was tribal knowledge, and gcp-0 rediscovered
  # it the hard way on 2026-08-28 with three failed recovery Jobs. CNPG refuses to
  # start a restored cluster whose DESTINATION WAL archive is non-empty --
  #
  #   barman-cloud-check-wal-archive: WAL archive check failed for server
  #   xplane-zitadel-cnpg-cluster: Expected empty archive
  #
  # -- because a restore opens a new timeline that would collide with the WALs
  # already there. This bucket outlives the cluster by design, so the destination
  # is never empty on a rebuild and the step is a normal part of restoring rather
  # than an incident.
  #
  # `scripts/cnpg-prepare-restore.sh` is that step with a guard: it refuses unless
  # the dated seed actually holds a base backup, so the live archive is never
  # cleared when there would be nothing to restore from. The manual equivalent is
  #   aws s3 rm --recursive \
  #     s3://<region>-ogenki-cnpg-backups/xplane-zitadel-cnpg-cluster/
  #
  # The durable fix is a per-generation serverName in the SQLInstance Composition,
  # which removes the step on both clouds. See docs/guides/restore-a-database.
  objectStoreRecovery:
    bucketName: "${region}-ogenki-cnpg-backups"
    path: "zitadel-20260829"
  backup:
    schedule: "0 0 * * *"
    bucketName: "${region}-ogenki-cnpg-backups"
  crossplane:
    compositionRef:
      name: xsqlinstances.cloud.ogenki.io

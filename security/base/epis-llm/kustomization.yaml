apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: security

# LLM-specific EPIs gated by the LLM umbrella Flux Kustomization
# (see clusters/mycluster-0/llm-platform.yaml).
resources:
  - llm-models-preload.yaml

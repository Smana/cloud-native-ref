---
# vLLM Semantic Router (Iris v0.1) — multi-signal classifier + jailbreak/PII/
# semantic-cache plugins. The entry point for inbound LLM traffic; routes
# to the per-claim XInferenceService backends.
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: vllm-semantic-router
  namespace: flux-system
spec:
  prune: true
  interval: 5m0s
  timeout: 10m0s
  path: ./infrastructure/base/vllm-semantic-router
  sourceRef:
    kind: ExternalArtifact
    name: infra-artifact
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: eks-aws-0-vars
  dependsOn:
    # vmservicescrape.yaml needs the VMServiceScrape CRD, shipped by the
    # always-on `crds` Kustomization. Applied from bootstrap now (this used to
    # be ordered after crds implicitly, as a child of llm-platform).
    - name: crds
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: vllm-semantic-router
      namespace: llm

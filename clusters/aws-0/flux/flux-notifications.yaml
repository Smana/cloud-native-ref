apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-notifications
  namespace: flux-system
spec:
  prune: true
  interval: 1m0s
  path: ./flux/notifications
  sourceRef:
    kind: ExternalArtifact
    name: flux-artifact
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: eks-aws-0-vars
  dependsOn:
    # security-openbao, NOT security: externalsecret-flux-slack-app.yaml reads
    # through the ClusterSecretStore that ./security/aws-0/openbao creates.
    - name: security-openbao

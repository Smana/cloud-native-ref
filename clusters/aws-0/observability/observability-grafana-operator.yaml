apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: observability-grafana-operator
  namespace: flux-system
spec:
  interval: 3m0s
  timeout: 3m0s
  # NOT base/: this overlay adds the Karpenter dashboard, which is EKS-only.
  path: ./observability/aws-0/grafana-operator
  prune: true
  sourceRef:
    kind: ExternalArtifact
    name: observability-artifact
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: eks-aws-0-vars
  dependsOn:
    - name: observability-victoria-metrics-k8s-stack
